{
    executable: {{{ROOT_DIR}}}/bin/tezsign
    args: [
		{{{ROOT_DIR}}}/__bin_generated/tezsign.service.sh
    ]
    environment: {
        HOME: {{{ROOT_DIR}}}/data
    }
    restart: always
    restart_delay: 3
    stop_timeout: 300
    user: {{{USER}}}
}